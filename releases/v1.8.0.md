## What's New

### 🎨 Sidebar Redesign (shadcn sidebar-07 style)
- Collapsed sidebar now renders an icon-rail with visible button boxes (subtle background + border per item)
- Group spacing (`mt-4`) replaces the old `h-px` divider lines between nav groups
- Icon size tuned to `18px`, item height `h-8`, sidebar width `w-14` (56px) — matches the shadcn reference design
- User avatar shown in collapsed footer with tooltip (name + email on hover)

### 🙈 Hide WP Admin Menu Toggle
- New `PanelLeftClose` / `PanelLeftOpen` button in the plugin header (expanded) and footer (collapsed)
- Clicking hides `#adminmenumain` + `#adminmenuback` and removes the `margin-left` from `#wpcontent` — giving the plugin full-width screen space
- State persists in `localStorage` across page loads
- Tooltip shows "Hide WP menu" / "Show WP menu"

### 📸 Updated Screenshots
- All 14 README screenshots retaken with the new sidebar UI at 3228×1524px (2x retina)
